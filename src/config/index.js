const config = {
  urlApi: process.env.URL_API,
  urlAuth: process.env.URL_AUTH,
  limit: process.env.LIMIT,
  clientId: process.env.CLIEN_ID,
  clientScret: process.env.CLIENT_SECRET,
};

export default config;
